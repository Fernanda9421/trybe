const upperCase = (string) => {
  return string.toUpperCase();
}

const firstLetter = (stringTwo) => {
  return stringTwo[0]
}

const concat = (a, b) => {
  return `${a}${b}`;
}

module.export = {
  upperCase,
  firstLetter,
  concat,
};
